const state = {
    page: 1 // 當前頁碼
}

const actions = {}

const mutations = {}

const getters = {
    getPage: state => state.page
}

export default {
    namespaced: true,
    state,
    actions,
    mutations,
    getters
}
